# {{PROJECT_NAME}} 编码规范

> 本文档为 Aider / AI 编程助手提供项目特定的编码规范指导。
> 所有代码生成和修改必须遵循以下规则。

## 一、技术栈与架构

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| <!-- [CUSTOMIZE] 填写技术栈 --> |  |  |
| 前端框架 | Vue / React / Angular / Svelte | 版本号 |
| 构建工具 | Vite / Webpack / Rollup | 版本号 |
| 类型系统 | TypeScript / JavaScript | 版本号 |
| 状态管理 | Pinia / Redux / Zustand | 版本号 |
| 样式方案 | TailwindCSS / SCSS / CSS-in-JS | 版本号 |
| 后端/运行时 | Node.js / Python / Go / Rust | 版本号 |
| 数据库 | SQLite / PostgreSQL / MySQL | 版本号 |

## 二、项目结构

```
{{PROJECT_NAME}}/
├── src/                    # 源码目录
│   ├── components/         # 组件
│   ├── composables/        # 组合式函数 / Hooks
│   ├── services/           # 服务层
│   ├── stores/             # 状态管理
│   ├── types/              # 类型定义
│   ├── utils/              # 工具函数
│   └── views/              # 页面视图
├── tests/                  # 测试文件
├── docs/                   # 文档
└── ...
```

## 三、命名规范

### 3.1 文件命名

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件 | PascalCase | `UserProfile.vue` |
| 组合式函数/Hooks | camelCase, useXxx 前缀 | `useAuth.ts` |
| 工具函数 | camelCase | `formatDate.ts` |
| 类型定义 | PascalCase | `UserTypes.ts` |
| 常量 | UPPER_SNAKE_CASE | `API_CONFIG.ts` |
| 样式文件 | kebab-case | `user-profile.css` |

### 3.2 代码命名

- 变量/函数：camelCase
- 类/接口/类型：PascalCase
- 常量：UPPER_SNAKE_CASE
- 私有成员：_prefix 或 #private
- 布尔值：isXxx / hasXxx / canXxx / shouldXxx 前缀

## 四、TypeScript / JavaScript 规范

### 4.1 类型定义

- 所有函数参数和返回值必须标注类型
- 优先使用 `interface` 定义对象类型
- 联合类型使用 `type`
- 避免使用 `any`，使用 `unknown` + 类型守卫

```typescript
// Good
interface User {
  id: string
  name: string
  email: string
  status: 'active' | 'inactive' | 'pending'
}

// Bad
function processUser(user: any) { ... }
```

### 4.2 错误处理

- 异步函数使用 `try/catch`
- 服务层返回 `{ data: T } | { error: string }`，不跨层抛异常
- 禁止静默吞掉错误

```typescript
async function fetchUser(id: string): Promise<{ data: User } | { error: string }> {
  try {
    const user = await api.getUser(id)
    return { data: user }
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err)
    console.error('Failed to fetch user:', message)
    return { error: message }
  }
}
```

### 4.3 空值检查

- 使用可选链 `?.` 和空值合并 `??`
- 禁止非空断言 `!` 除非绝对确定

```typescript
// Good
const name = user?.profile?.name ?? 'Anonymous'

// Bad
const name = user!.profile!.name
```

## 五、组件/框架规范 [CUSTOMIZE]

### 5.1 Vue 3 示例

```vue
<script setup lang="ts">
// 1. 类型导入
// 2. Vue / 第三方库导入
// 3. 项目内部导入
// 4. Props 定义
// 5. Emits 定义
// 6. 组合式函数
// 7. 响应式状态
// 8. 计算属性
// 9. 方法
// 10. 生命周期
// 11. 监听器

interface Props {
  title: string
  visible?: boolean
}

const props = withDefaults(defineProps<Props>(), {
  visible: false
})

const emit = defineEmits<{
  close: []
  confirm: [value: string]
}>()
</script>
```

### 5.2 React 示例

```tsx
import { useState, useCallback } from 'react'

interface Props {
  title: string
  onConfirm?: (value: string) => void
}

export function MyComponent({ title, onConfirm }: Props) {
  const [count, setCount] = useState(0)

  const handleClick = useCallback(() => {
    setCount(c => c + 1)
  }, [])

  return <div>{title}</div>
}
```

## 六、样式规范 [CUSTOMIZE]

### 6.1 TailwindCSS

- 优先使用 Tailwind 工具类
- 复杂样式提取为组件类
- 使用 `@apply` 仅在必要时

```html
<!-- Good -->
<div class="flex items-center gap-4 p-4 bg-white rounded-lg shadow">

<!-- Bad -->
<div class="custom-card">
```

### 6.2 CSS 命名（BEM）

```css
.block { }
.block__element { }
.block--modifier { }
```

## 七、状态管理规范

### 7.1 Pinia (Vue)

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'

export const useXxxStore = defineStore('xxx', () => {
  const state = ref<State>(initialState)
  const derived = computed(() => ...)

  async function action() {
    // ...
  }

  return { state, derived, action }
})
```

### 7.2 数据流原则

- 单一数据源
- 状态只读，通过 actions 修改
- 异步逻辑放在 actions 中
- 避免直接修改 store 状态

## 八、API / 后端规范 [CUSTOMIZE]

### 8.1 请求封装

```typescript
// 统一错误处理、超时、重试
async function apiRequest<T>(url: string, options?: RequestInit): Promise<T> {
  const response = await fetch(url, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options?.headers
    }
  })

  if (!response.ok) {
    throw new ApiError(response.status, await response.text())
  }

  return response.json()
}
```

### 8.2 错误码规范

| 状态码 | 含义 | 处理 |
|--------|------|------|
| 400 | 请求参数错误 | 提示用户检查输入 |
| 401 | 未授权 | 跳转登录 |
| 403 | 禁止访问 | 提示权限不足 |
| 404 | 资源不存在 | 提示或忽略 |
| 500 | 服务器错误 | 提示稍后重试 |

## 九、测试规范 [CUSTOMIZE]

- 测试文件与源文件同级或放在 `__tests__` 目录
- 命名：`Component.test.ts` 或 `Component.spec.ts`
- 覆盖 happy path + 至少一个错误场景 + 一个边界条件

```typescript
describe('UserService', () => {
  it('should fetch user successfully', async () => {
    // Arrange
    // Act
    // Assert
  })

  it('should handle network error', async () => {
    // ...
  })

  it('should handle empty response', async () => {
    // ...
  })
})
```

## 十、Git 提交规范

### 10.1 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**type 类型**：
- `feat`: 新功能
- `fix`: 修复
- `perf`: 性能优化
- `refactor`: 重构
- `style`: 样式调整
- `docs`: 文档更新
- `test`: 测试相关
- `chore`: 构建/工具

**scope 范围**：[CUSTOMIZE] 填写项目模块名

### 10.2 示例

```
feat(auth): 添加 OAuth2 登录支持

- 集成 Google 和 GitHub 登录
- 添加登录状态持久化
- 实现 Token 刷新机制

fix(api): 修复分页参数越界

perf(list): 优化大数据量渲染性能
```

## 十一、开发工作流检查清单

修改代码前：
- [ ] 评估影响范围，确保不破坏相邻模块
- [ ] 优先增量调整，避免不必要的重构
- [ ] 修改公共模块时验证所有调用方

修改代码后：
- [ ] 运行构建/类型检查通过
- [ ] 遵循项目样式规范
- [ ] 对修改功能做人工验证
- [ ] 更新相关文档

## 十二、禁止事项 [CUSTOMIZE]

1. **禁止**使用 `any` 类型 — 使用 `unknown` + 类型守卫
2. **禁止**在组件中直接调用 API — 使用服务层封装
3. **禁止**直接修改 props — 使用 emit 或本地状态
4. **禁止**跨层抛异常 — 服务层返回错误对象
5. **禁止**静默吞错误 — 所有错误必须记录或通知
6. **禁止**提交包含 `console.log` 的代码 — 使用日志库
7. **禁止**硬编码魔法数字/字符串 — 提取为常量
8. **禁止**在循环中做 DOM 操作 — 使用文档片段或虚拟列表

---

> **使用说明**：
> 1. 复制此模板到项目根目录，重命名为 `CONVENTIONS.md`
> 2. 替换 `{{PROJECT_NAME}}` 为实际项目名称
> 3. 删除或修改标注 `[CUSTOMIZE]` 的部分以匹配实际技术栈
> 4. 根据项目实际情况增删规范条目
> 5. 在 `.aider.conf.yml` 中配置 `read: [CONVENTIONS.md]` 使其自动加载
